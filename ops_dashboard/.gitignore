# Isolated GUI venv — never committed (I4: no broker packages leak in either)
.venv/
__pycache__/
*.pyc
.pytest_cache/
# Local auth secrets may be written into gui_config.yaml by `--setup`; keep the
# committed copy free of real secrets (the committed file ships empty auth fields).
