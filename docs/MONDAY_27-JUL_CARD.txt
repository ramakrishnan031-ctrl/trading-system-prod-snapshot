==============================================================================
MONDAY 27-JUL-2026 — THE CARD.   Detail: MASTER_PENDING_..._EVENING.txt
Deployed d0fd7b8 · loads 08:15 · book flat.  Every command below was RE-RUN against
production 26-Jul 16:1x (schema v44, trades 423). Two originally-drafted checks did
not exist — see footnote.
    ssh trading-vm ; cd ~/systems/trading-system ; L=logs/system_$(date +%F).log
    DB="file:data_store/trading_system.db?mode=ro&immutable=1"
==============================================================================
08:15  — THREE CHECKS, TWO MINUTES
------------------------------------------------------------------------------
[ ] 1 ⭐⭐ PB-01 IS WIRED — the highest-value 30 seconds of your week.
      grep "PB-01 watchlist" $L
      EXPECT ... PB-01 watchlist: ENABLED — capture + entry stage started
      ⚠️ DISABLED or NOTHING => the 17:00 capture WILL FAIL, and you have ALL DAY
         to react instead of discovering it Tuesday.

[ ] 2 PRIOR-DAY KILL CLEARED.
      journalctl -u trading-system --since today | grep auto-cleared
      EXPECT  Kill switch auto-cleared: prior SOFT_KILL from 2026-07-24 ...
      ⚠️ ABSENT + kill still set => entries blocked all day.

[ ] 3 SHADOWTRACKER OFF — proved by silence; nothing logs the flag.
      sqlite3 "$DB" "select count(*) from innings;"
      EXPECT 322, and 322 forever. Growth => the disable did not take.

⛔ Do NOT look for the 17:35 window this morning — no such boot line exists
   (the guard runs before config loads). It shows up at ~17:35 - CHECK 7.

~17:10 — THE MOST IMPORTANT CHECK OF THE DAY
------------------------------------------------------------------------------
[ ] 4 systemctl is-active trading-system            EXPECT active
      ⭐ 16:00-17:35 is a MONITORING BLIND SPOT (liveness probe stops 15:55); this
      is the compensating control. ⚠️ A silent death at 16:20 yields an empty
      Tuesday watchlist that looks EXACTLY like "no breakouts" — after the fact
      the two are indistinguishable.

AFTER 17:00 — TWO CHECKS. DIFFERENT FACTS. BOTH NEEDED.
------------------------------------------------------------------------------
[ ] 5 DID THE ALERT ARRIVE?
      sqlite3 "$DB" "select status,executed_at from cron_heartbeat
                      where job_name='pb01_capture' order by id desc limit 3;"
      ⭐ VERIFIED 26-Jul: this returns ZERO ROWS today — there has never been a
         pb01_capture heartbeat. So Monday's would be the FIRST EVER, and an empty
         result tonight is the honest "it did not run", NOT a stale query.
[ ] 6 DID THE WORKER FINISH?
      sqlite3 "$DB" "select count(*) from pb01_watchlist;"   (still 0, re-checked 26-Jul)
      ⚠️ The heartbeat records QUEUED, not captured — the worker is async.
         HEARTBEAT-BUT-NO-ROWS IS A REAL STATE (alert landed, worker failed).
[ ] 7 ~17:40  grep eod_self_exit $L
      EXPECT  past 17:35 IST and flat ... clean shutdown   (Friday said 16:00)

IF YOU EVER NEED TO HALT — READ THIS BEFORE THE MOMENT, NOT DURING IT
------------------------------------------------------------------------------
⛔⛔ `halt.sh` DOES NOT EXIST. It was a hypothetical in an F2 report and was never
   built. VERIFIED: not in the repo (nor in its history), not on the VM, not on
   PATH. If you reach for it in an incident it will fail.
   (`deploy/resume.sh` DOES exist, with no counterpart — that asymmetry is
   exactly what makes the wrong assumption easy.)

*** THE ONLY HALT THAT WORKS TODAY: ***
      ssh trading-vm sudo systemctl stop trading-system

⚠️ AND KNOW WHAT IT COSTS: it stops EVERYTHING — entries AND exit management.
   There is NO way today to block new entries while still managing open
   positions. If you stop the service holding an open position, that position is
   left to its BROKER-SIDE SL/TGT with no system monitoring.
   That is survivable — those are real resting orders at the broker, not
   system-simulated ones — but it is a real reduction in cover, and you should
   know it now rather than discover it then.
   To resume: deploy/resume.sh (clears a kill), then start the service.

EMAILS YOU WILL GET THAT MEAN NOTHING (so a real one still stands out)
------------------------------------------------------------------------------
✅ "UNEXPECTED SSH KEY present" — GONE. It fired 37 times since 17-Jul across two
   key rotations; the last was Sat 21:46:45. You re-baselined at 00:24 Sunday and
   the third (committed) baseline was fixed after that, so the check now produces
   NO finding at all. ⭐ If a 38th ever arrives, that is NEW — do not assume it is
   the old loop.
✅ AND IT CANNOT COME BACK AS A FLOOD. The 6-hourly re-alert was the real defect
   and it is fixed: a condition that is merely STILL PRESENT now backs off
   6h -> 24h -> 7d and is never CRITICAL twice. A condition that CLEARED and CAME
   BACK is now alerted immediately at full severity — it used to be swallowed.
⭐ "System Manager EOD — clean" IS NOT A CRITICAL. Yesterday's card said it was;
   that was wrong. Measured by re-rendering all 85 sentinels through the function
   that builds the subject: it arrives as "[LFL836] INFO — …". So do the Pre-flight
   READY and Cron Daily Report ones. They are correctly labelled already.
· STILL ROUTINE, still worth ignoring: the 15:15 "SOFT KILL ACTIVATED —
  circuit_breaker_force_close_15:15", same second every trading day, by design.
🔴 "RMS/MANUAL CLOSE — <symbol>" — ⚠️ DO NOT TRUST THE WORDING, AND EXPECT IT AGAIN
   MONDAY. All four sent so far (HUHTAMAKI, WAAREERTL, GODIGIT, BLSE) were the
   system's OWN SL/TGT/EOD order filling, not an external close — the reconciler
   polled between the broker fill and its own bookkeeping. The MONEY was right every
   time. ⭐ WORSE THAN NOISE: because the reconciler wins that race, our own exit
   path aborts at its double-close guard, so the INFO "TARGET HIT" is never sent —
   the false CRITICAL REPLACES the good alert.
   ⭐ THE FIX IS BUILT BUT DELIBERATELY NOT DEPLOYED FOR MONDAY. It sits on branch
   hold-check1-w8-26jul (unpushed) and ships TUESDAY only if Monday is observed
   clean — so Monday carries its four existing first-runs alone. Monday's behaviour
   is therefore UNCHANGED: if this email arrives, it is the known false label, not a
   new fault. See alert_stream_fixes_26jul2026 + check1_w8_design_26jul2026.
⚠️ If an email arrives that is NOT on this list, it is worth stopping for.

TUESDAY 08:15 — THREE NON-EVENTS
------------------------------------------------------------------------------
· CRITICAL sentinel  -> most likely the reconciler having run 95 min longer than
  ever (~380 extra broker calls, a state that has never existed). EXPECTED shape,
  self-clearing. Read it before reacting.
· EMPTY watchlist    -> no breakouts OR silent capture failure. Checks 5+6 now
  distinguish these; before this week they could not.
· REGRESSION count moved -> the evening time-bomb was FIXED 25-Jul; the count no
  longer depends on the hour. Residual wobble = the known machine-global
  test_instance_lock flake (2/1/2 on an unchanged tree). Judge by the failure SET
  against a same-shell baseline, NEVER by the count.
  ⭐ MEASURED AGAIN 26-Jul AND IT PROVES THE POINT: the same unchanged tree gave
  11F here where the recorded figure was 33F — a PATH difference, not a regression.
  The SET is the only thing that means anything.
  ⚠️ AND A NEW ONE, RECORDED NOT FIXED: test_daily_trade_review::test_main_defaults_
  date_to_today_and_records_heartbeat fails on WEEKENDS (the report correctly skips
  a Sunday). Same class as the hour-of-day bomb fixed 25-Jul; it is IN the weekend
  baseline, so it must not be read as a Monday regression.

⭐ WHAT MONDAY DOES *NOT* PROVE
------------------------------------------------------------------------------
A CAPTURE IS A PRECONDITION, NOT A WIN. PB-01 is enabled:false with NO live twin,
so a captured row cannot yet be evaluated. It starts an out-of-sample clock that
can only ever be STARTED, never backfilled — which is why a lost evening costs
something permanent, and a captured one proves nothing yet.
------------------------------------------------------------------------------
FOOTNOTE (why two drafted checks are absent, verified 25-Jul): there is NO boot
line naming the service window (guard main.py:1712 runs before load_all :1743)
and NOTHING logs shadow_tracker's flag anywhere — replaced by checks 7 and 3.
Also: the boot log is logs/system_<date>.log, NOT journald (~6 lines/boot); only
check 2 lives in journald.
==============================================================================
