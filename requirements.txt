# Trading System v2 — pinned runtime dependencies
# Update pins after each venv rebuild; record exact versions from `pip freeze`.
# Install: pip install -r requirements.txt

# ── Config loading ──────────────────────────────────────────────────────────
pyyaml==6.0.3          # CL5: yaml.safe_load for all YAML files
pydantic==2.13.0       # CL3: strict schema validation (extra="forbid")
kiteconnect==5.1.0     # Audit #11: pin; Kite SDK API compat
flask==3.1.3           # WR2: webhook HTTP server
waitress==3.0.2        # B.3 / Audit 6.5 + A.1 (2026-04-25): production WSGI server (cross-platform; replaces app.run; supersedes gunicorn ask -- waitress works on Windows dev workstation, gunicorn is UNIX-only)
openpyxl==3.1.5        # DR5: daily_review .xlsx output
python-dotenv==1.2.2   # runtime .env loading (SU19)
