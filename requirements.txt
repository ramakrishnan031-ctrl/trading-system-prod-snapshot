# Trading System v2 — pinned runtime dependencies
# Update pins after each venv rebuild; record exact versions from `pip freeze`.
# Install: pip install -r requirements.txt

# ── Config loading ──────────────────────────────────────────────────────────
pyyaml==6.0.3          # CL5: yaml.safe_load for all YAML files
pydantic==2.13.0       # CL3: strict schema validation (extra="forbid")
kiteconnect>=4.2.0
flask>=3.0               # WR2: webhook HTTP server
openpyxl>=3.1            # DR5: daily_review .xlsx output
python-dotenv>=1.2.2     # runtime .env loading (SU19)
